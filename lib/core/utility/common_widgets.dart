import 'package:flutter/cupertino.dart';
import 'package:scaffold/core/constants/dimen.dart';

height({double height = padding}) => SizedBox(
      height: height,
    );

width({double width = padding}) => SizedBox(
      width: width,
    );
